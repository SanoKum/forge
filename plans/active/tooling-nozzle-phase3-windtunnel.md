# ノズル設計ツール Phase 3: ①軸対称風洞 (モード F + 帰還エンジン v1/v2 + 照合)

## メタ

- **area**: `tooling / optimization`
- **status**: `in_progress`
- **related_docs**:
  - [`methods/design/overview.md`](../../methods/design/overview.md) (現在仕様)
- **related_plans**:
  - 親: [`tooling-nozzle-design-tool.md`](tooling-nozzle-design-tool.md) — §4.6①/§4.7 (2026-08-13 改訂 = 帰還 3 段構成) と §5 Phase 3 を実装する
  - 前提: [`../accepted/tooling-nozzle-phase0-foundation.md`](../accepted/tooling-nozzle-phase0-foundation.md) (評価パイプライン) / [`../accepted/tooling-nozzle-moo-loop.md`](../accepted/tooling-nozzle-moo-loop.md) (MOO 基盤)
  - 参考: [`../../notes/investigations/nozzle-top-internal-shock-diagnosis.md`](../../notes/investigations/nozzle-top-internal-shock-diagnosis.md) (forge/SU2 Euler が安価で信頼できる対照であることの確証)
- **created**: `2026-08-13`
- **owner**: `sano` (実装: Claude 自走)

## 1. 目的

①軸対称風洞ノズルを**モード F (中心線マッハ全権)** で設計できる状態にする。核心は
**帰還エンジンの初実装** (親計画 §4.7 の 3 段構成のうち v1・v2):

- **v1 (MOC 逆設計)**: starting line + 軸目標 $M_c(x)$ → 逆マーチで超音速場を充填 →
  **質量流量法**で壁流線を抽出 → **δ\* 経験式**の法線オフセット。CFD なし・ミリ秒
- **v2 (Euler 帰還)**: forge Euler (cell) で検算し、軸 $M(x)$ 残差を v1 特性線網の
  **凍結対応マップ**で壁へ帰還。$\|\Delta M\|_\infty \le 0.5\%\,M_d$ までパス反復

検証は「既知目標分布 → 逆設計 → forge 実測 → 目標回復」の自己一貫 + 一様性
$\varepsilon_M,\varepsilon_\theta$ の実装。MOO (co-kriging 多忠実度)・凝縮確認メニューは
本 plan の後半ステップ (7–8) とし、v1/v2+照合の成立を先に確定する。

## 2. スコープ

- **やる**:
  - `probdef` に `wind_tunnel_axisym` 型 (spec: $P_0,T_0,D_e,M_d$ / ($D_e,r^*,M_d$) 過拘束
    チェック / $r^*$ 閉ループ派生 — 初回は無次元 $r^*{=}1$ 設計 → $D_e$ スケーリング)
  - **逆 MOC マーチ** (`geometry/moc_inverse.py`): 軸データ (M 指定, θ=0) からの横マーチ
    (Goursat) による場充填 + 各 C⁺ フロントの $\int\rho u\,dA$ = スロート流量で壁点抽出。
    軸目標定義域は $M_d$ 到達後「一定」延長 (§4.7(c) — 壁端 C⁻ 足の充足は一様域で自明)
  - モード F 目標 Bézier: starting line の軸点に C2 整合、出口 $(M_d, M'{=}M''{=}0)$ C2
  - δ\* 経験式 (`feedback/deltastar.py`): 圧縮性乱流平板相関の局所適用 (Edenfield 系は option)。
    v1 の役割は初期壁の質だけなので相関精度は帰還収束速度にのみ影響 (B3)
  - **v2 Euler 帰還** (`feedback/euler_loop.py`): forge Euler (cell — node slip 市松の既知
    問題を回避)・R1 同トポロジ再メッシュ・同 index warm restart・v1 特性線網の凍結
    軸↔壁マップ・PM 換算 $\Delta\theta_w=\omega\Delta\nu$・重みランプ・クリップ (§4.7(c))
  - 一様性メトリクス: $\varepsilon_M$ (コア質量流束重み RMS)・$\varepsilon_\theta$ (コア max 流れ角)
  - **照合**: (i) 自己一貫 (既知目標→回復)、(ii) 放射源流・一様流の解析照合 (単体)、
    (iii) 可能なら CONTUR/Sivells 系コンタ (papers/ の AEDC-TR-78-63) との比較
- **後半 (ステップ 7–8, v1/v2 成立後)**:
  - モード F dv ($M_c$ 自由 CP k=3 + $L$ + $R$) の MOO、Euler=低忠実度の co-kriging
  - 凝縮確認メニュー (複数入口条件 → 出口 M・動圧低下表)
- **やらない**:
  - v3 (NS 凍結場フルトレース) — v2 停滞時のみ (§4.7)
  - Kliegel–Levine 高次 — ① は $R_u{=}R_d$ 単一円弧で $R\gtrsim1$ が標準、Sauer で足りる
    見込み (着手時に実値で要否判定、必要なら本 plan に追記)
  - ② 矩形 (側壁 δ\*)・q_peak ランキング検証 (Phase 3 後半〜Phase 4)

## 3. 関連 docs と前提

- 帰還アルゴリズムの正本 = 親計画 §4.7 (a)–(c)。逆マーチの数理は本セッションの議論で
  確定済み: 壁点の依存域は軸の両側 (C⁺ 足上流・C⁻ 足下流)、一様出口では下流延長が自明
- `moc_kernel.py` の実装知見 (楔充填・軸対称 1/r 源項の評価点・前フロント補間) を流用
- forge Euler = `viscMethod: 0, visc: 0.0, thermCond: 0.0` + slip 壁 + cell (夜間バッチ
  run_0085 で検証済みの経路)。SU2 Euler 対照も安価 (rms 10⁻¹¹, ~3 分)

## 4. 設計方針 (差分のみ)

- **スロート近傍の幾何**: ① は $R_u=R_d=R$ 単一円弧 (dv, 初期 1.5–2)。starting line は
  既存 Sauer。壁の所有権: 円弧は「遷音速アンカー」(モード F — 円弧終端 = starting line
  が壁に当たる点、θa という dv は無い)
- **逆マーチの検証規律** (moc_kernel と同格): 一様流 (M 一定 → 円筒壁)・放射源流
  (解析コーン回復) の 2 解析照合 + 格子収束。決定性 (同一入力 → bit 同一壁)
- **v2 の収束判定は実場**: 達成軸 M を固定相対格子に補間し $\|\Delta M\|_\infty$。マスク:
  $M<1.05$ 域・出口近傍ランプ (§4.7(c))
- case は `case/41.wind_tunnel_design/` を新設 (README run 台帳)

## 5. 実装ステップ

1. `geometry/moc_inverse.py`: 逆マーチ + 質量流量法壁抽出 + 単体検証 (一様流/源流/格子収束)
2. モード F 目標 Bézier 構成 (starting line C2 整合) + `probdef` `wind_tunnel_axisym` 型
3. `feedback/deltastar.py` (δ\* 相関) + v1 チェーン結合 (dv → 物理壁 → TFI メッシュ)
4. **v1 E2E**: $M_d=4$ 標準条件で逆設計壁を forge Euler 実測 → 達成軸 M vs 目標 (期待
   ~1–3% 残差) + $\varepsilon_M,\varepsilon_\theta$ 実装
5. `feedback/euler_loop.py` (v2): 凍結マップ帰還のパス反復 → $\|\Delta M\|_\infty \le 0.5\%M_d$
6. 照合: 自己一貫 (別の既知分布から回復) + Sivells 系比較 (可能なら)
7. (後半) モード F MOO (co-kriging: Euler=低 / NS=高)
8. (後半) 凝縮確認メニュー
- 各段で `methods/design/overview.md` を同期

## 6. 検証

- 単体: 逆マーチの解析照合 2 系統 + 格子収束 + 決定性 (§4)
- v1: 逆設計壁の forge Euler 達成度 (定量記録)。メッシュ品質 PASS
- v2: 帰還収束履歴 ($\|\Delta M\|_\infty$ vs パス数) がロバストに単調減、~10 パス内で
  0.5% 到達。R1 restart の過渡長実測 (親計画 §6 Phase 1 の宿題)
- 一様性: 収束解で $\varepsilon_M, \varepsilon_\theta$ を報告 (NS でも 1 本測る)
- 台帳: `case/41` README + 3 ツール VERDICT

## 7. 影響範囲

- 新規: `design/forge_design/geometry/moc_inverse.py`, `design/forge_design/feedback/`,
  `case/41.wind_tunnel_design/`
- 変更: `probdef.py` (問題型追加)・`evaluate/runner.py` (Euler 実行モード・wind tunnel 対応)
- 既存ソルバ無改造

## 8. 完了条件

- [ ] §5 の 1–6 (v1/v2 + 照合)
- [ ] §5 の 7–8 (MOO + 凝縮メニュー) — 先行完了条件を分けて中間コミット可
- [ ] `methods/design/overview.md` 同期
- [ ] status done → `accepted/`、`plans/README.md` 同期

## 9. スロート直後の圧力波 — 訂正済みの作戦 (2026-08-14, Codex レビュー反映)

### 9.1 撤回・訂正した主張 (すべて私の誤り)

| 主張 | 判定 | 正しい理解 |
| --- | --- | --- |
| fill の向きを「修正」した | **撤回** | 楔を*誤ったデータ*で埋め、出口 ε_M が 0.173→0.932% に悪化 (**この数値は旧・非軸対称重みの暫定指標。A2 後に再測定するまで確定値として使わない**)。旧実装 (楔は空・凸包補間) を維持 |
| 曲率ブレンドで C2 を作る | **撤回** | 健全な設計データを 1.2 $r^*$ 上書きし更に悪化 (→1.166%、同じく**暫定指標**)。既定 off、呼び出し側 fallback も 0.0 |
| 「壁帯が原理的に計算できない」 | **誤り** | 実測すると計算点は壁より上まで及ぶ (デモ図は軸データが短い粗解像のための誤解) |
| 「$p,\rho,V$ 連続 → 曲率連続。C+ マーチで自動一致」 | **誤り** | $\partial p/\partial n=\rho V^2\kappa$ より、必要なのは**法線圧力勾配の連続**。C2 接続は初期データの整合条件か形状表現で**別途保証**が要る |
| 「下流壁は形状表現を持たない」 | **誤り** | not-a-knot `CubicSpline` という明確な表現。壁足 θ 上書きはスプライン端微分を拘束せず、C1 は**事後検査**にすぎない |
| 「$M''$ を合わせても効かない」 | **誤り** | スプライン端 (not-a-knot) のアーティファクトを測っていた。清浄位置で再測すると**現行の目標は $M''(x_0)=-0.483$ に対し Sauer は $+0.326$ = 符号違い** |
| 「node 化すれば v3 マップは自然に動く」 | **誤り** | `build_cminus_map_cfd` は常に `CELLS/centCoords` を読む。node では VALUE 配列長が node 数になり不整合 |

### 9.2 作戦 (この順で進める。**A が完了するまで計算を投入しない**)

**A. 実験条件・測定の健全化**
- A1 blend を真に無効化 (呼び出し側 fallback 0.0) + 問題 YAML に明示。**汚染 run (0004–0007) は削除も再利用もせず台帳に「破棄予定」として記載**し、以後は必ず**新しい連番 (run_0008 以降)** で投入する (既存 run を使い回さない — AGENTS ルール。初回 0006/0007 を同名で作り直したのは違反だった)
- A2 **完了 (2026-08-14)**: $\varepsilon_M$ の幾何を**出口境界面の実 connectivity**
  (`BCONDS/2/iPlanes` + `PLANES/surfArea`) から取るよう書き換え。軸対称では面中心が
  半径中点なので $2\pi r_c dr=\pi(r_{out}^2-r_{in}^2)$ が**厳密** (実測 max rel 2.5e-5、
  総和は $\pi r_{wall}^2$ と 1e-5 で一致) — 仮想境界は不要になった。壁半径も最外面の
  外縁 = **真の壁半径** (最外セル中心 0.032561 → 0.032643、0.25% 差)。固定半径格子への
  補間積分も `n_grid` で交差検証でき、面積積分と一致 (0.173% vs 0.173%)。
  **テストコア = 有効菱形の上流境界を実場で C⁺ トレース**して決める
  (`core_radius_traced`)。直線近似 $(x-x_d)\tan\mu_d$ は特性線が一様域でしか直線でない
  ため近似で、実測は トレース 0.835 vs 直線 0.824 (0.7% 差)。**「リップ平面でちょうど壁に
  達する」という旧 docstring の主張は誤りで、コアは壁半径に届かない**ことを確認・訂正した
- A3 **各パスに CFD 健全性ゲート** (既存ルールを弱めない): `check_convergence.py` の VERDICT を**必須**として記録し、加えて報告量の `check_quasisteady.py`・残差の絶対レベル・時系列を**併用**する。**NOT CONVERGED を独自ゲートで「収束相当」に読み替えない** — 未収束なら未収束と明記し、その場を「収束場」と呼ばない

**B0. 遷音速 starting line の壁角 — 観測と、既定変更の撤回 (2026-08-14)**
- 観測: Sauer 解は線形化壁 BC $\bar v=dr_w/dx$ を満たすのに `theta()` は
  $\operatorname{atan2}(\bar v,1+\bar u)$ なので、壁足の θ は円弧接線より 25.7% 小さい
  (R=2 で 5.892° vs 7.394°)。$\theta=\arctan\bar v$ にすると 7.639° と近づき、
  リップ半径 / 1D√ε も 0.9964→0.9990 になる。
- **しかし既定化は撤回** (レビュー指摘): (i) `mach()` は速度ベクトル $(1+\bar u,\bar v)$
  から M を出すので、$\theta=\arctan\bar v$ にすると **M と θ が別の速度ベクトルを
  指す**不整合が生じる。(ii) **壁足 θ は `th_wall0` で円弧接線に上書き済み**なので
  「starting line が 1.5° のキンクを直接渡していた」という説明も不正確。
  → 既定は `exact_ratio` に戻し、`linearized` は**実験オプション**として保持。
- 「**Kliegel–Levine が必須**」という結論も**現時点では導けない** (撤回)。一次資料
  照合と整合試験 (M と θ の整合・格子収束) を経てから判断する。

**B. C2 接続を保証する (本丸)**
**B1 と B2 は別の対象を保証する** (混同しない):

- **B1 = 中心線 Cauchy データの C2 整合**: starting line における目標 $M_c$ の $M''(x_0)$ を Sauer に整合させる (Sivells の古典要件 — サーベイ A0/B2)。現行は $-0.483$ vs $+0.326$ の符号違い。CP を 1 個増やし探索次元は不変に保つ。
  **これは中心線データの整合であって、壁の C2 を保証するものではない** (仮説であり確定ではない)
- **B2 = 円弧と壁形状の幾何学的 C2 接続**: 始端で $r,r',r''$ を円弧に一致させた B-spline を逆 MOC 点列に**最小二乗フィット**。バッファ領域を作らず、形状表現そのものに C2 を内蔵する
- **壁足の接線跳びの原因を確定した (2026-08-14 診断、以前の「楔未計算が原因」は撤回)**:
  1. **被覆図**: 楔 (starting line 直後・壁近傍) は**密に計算されており空隙はない**。
     壁流線は補間の空白を跨いでいない (`case/41/wedge_coverage.png`)。
  2. **解像度収束**: `n_start` 41→81、`n_axis` 400→800、`dx_wall` 0.02→0.01 のいずれでも
     壁足 $\tan\theta[1]$ は **0.071–0.102 で振れるだけで円弧の 0.1298 に近づかない**
     (一方 $r(x_0{+}0.2)$・リップ半径は 4 桁収束)。→ **離散化誤差ではない**。
  3. **$R$ スイープで摂動オーダーを確認** (決定打):

     | $R$ | $\bar u$(壁) | Sauer $\theta_w$ | 円弧 $\theta$ | 不整合 | 設計壁 $\tan\theta[1]$/円弧 |
     | --- | --- | --- | --- | --- | --- |
     | 2 | 0.300 | 5.892° | 7.394° | 1.502° | 0.578 |
     | 5 | 0.142 | 2.262° | 2.559° | 0.297° | 0.919 |
     | 10 | 0.091 | 1.208° | 1.313° | 0.105° | 1.032 |
     | 20 | 0.066 | 0.708° | 0.753° | 0.045° | 1.131 |

     相対不整合は $\bar u$ にほぼ比例 (比 0.68→0.90 で 1 に漸近) — $\theta=\arctan(\bar v/(1+\bar u))$
     と線形化壁 BC $\bar v=dr_w/dx$ の差が $O(\bar u)$ である事実と**定量的に一致**。
  4. **結論**: 真因は **Sauer 一次遷音速解の精度**。$R=2$ では starting line の壁で
     $\bar u\approx0.30$ と摂動が小さくなく、壁流れ角に 26% の誤差が出る。
     **C⁺ マーチ (壁帯被覆) でも B2 (壁の表現) でも直らない** — 前者は被覆問題を解く
     ものだが被覆は既に足りており、後者はデータに反する曲率を押し付けるだけ。
  5. **選択肢** (どちらを採るかは設計判断):
     - (a) **Kliegel–Levine 高次遷音速解**を実装する — $R$ の制約が外れる正攻法
     - (b) **① を $R\gtrsim5$ に限定**する (不整合 0.3° 以下、壁足も 8% 内)。風洞は
       元々スロート曲率が大きい方が一様性に有利なので実用上は成立し得る。ただし
       $R$ は dv なので探索範囲に下限制約が付く
- **B3 効果の分離は 2×2 で行う**: (現行) / (B1 のみ) / (B2 のみ) / (B1+B2) の 4 条件を、A 完了後の測定器と収束ゲートの下で比較し、壁曲率の跳びと圧力波の消失を判定する

**B4. 接合部を壊さない狭幅マスク A/B (2026-08-14 — 「初期形状は帰還が直す」前提の再確認)**
- **前提の再確認 (ユーザ指摘が正しい)**: 初期形状が多少悪くても、帰還がその場所を
  動かせて誤差を観測でき反復が収束するなら最終形状は直る。実測でも**中域 (重み 1.0)
  は $|\Delta M|$ 0.1156→0.0180 (6 倍改善)** の一方、**接合帯 (重み 0.00–0.18) は
  0.1176→0.1168 とほぼ不変** — これは「Sauer 誤差が帰還不能」ではなく
  **「帰還をほぼ掛けていなかった」**と読むべき。KL / 遷音速 CFD へ進む前にマスクを疑う。
- **ただしマスクを単純にゼロにしない**: 円弧は固定・下流壁は可動なので、接合点で
  $\Delta r=\Delta r'=\Delta r''=0$ を保ったまま**直後の曲率だけ**変えられる必要がある。
  流れ角で与えるなら $\Delta\theta(x_j)=\Delta\theta'(x_j)=0$、すなわち
  $\Delta\theta=O((x-x_j)^2)$ で立ち上げる (`junction_taper`, 既定 quintic)。
- **実装で判明した別バグ**: 旧 `_rebuild_wall` は $r$ を `np.gradient` で微分して
  $\theta$ を作り再積分しており、この往復が **接合点の $r''$ を 0.50→0.25 と半減**させ、
  **解像度に依らなかった** ($r$ の $O(h^2)$ 誤差が 2 階微分で $O(1)$ になるため)。
  → $\theta$ を**主変数として持ち回る**形に変更。補正ゼロの往復誤差は
  $\Delta r''=9\times10^{-12}$ (n=1000) / $-9\times10^{-11}$ (n=4000) まで落ちた。
- **接合保存の数値検証** (テーパ幅 0.3, quintic): $\Delta r(x_j)=0$ 厳密、
  $\Delta r'(x_j)$ = −2.3e-6 → −5.4e-8、$\Delta r''(x_j)$ = −1.1e-3 → −7.5e-5
  ($h$ とともに消える)、一方 下流 $x_j+2$ では $\Delta r=+0.0516$ と補正は効いている。
- **正式 A/B の前提を確立**: 単一固定壁で `check_convergence.py` **PASS / ALL STEADY**
  になる設定を確認 (`run_0008_conv_single`: soft 段を挟まず準 1D IC から**単段**
  24000 step・cfl 4)。従来の 2 段起動は本段の初期残差が既に小さく「3 桁低下」条件を
  構造的に満たせなかった。
- **A/B の条件** (run_0009 以降・新連番): (i) 現行広幅マスク = 対照、(ii) 狭幅
  (円弧に着地する station のみマスク) + 接合テーパ。両者とも単段 PASS 設定・正式ゲート。
  → 結果を見てから KL / 遷音速 CFD の要否を判断する (**事前に既定路線を固めない**)。

**B4 の結果 (2026-08-15, run_0015/0016 — マスク単独では直らない)**
- **共通のマスクなし軸 M** で比較 (dM_inf_masked は wide/narrow で評価集合が違い
  A/B に使えない — Codex 指摘。以後は固定窓の共通指標を history に記録):
  中域 (x/rt 4–12) は wide 0.1165→**0.0631** / narrow 0.1149→**0.0353** と
  narrow が約 2 倍良い。**接合帯 (0.56–2.5) は wide 0.1177→0.1176 /
  narrow 0.1177→0.1154 とほぼ不変**。
- **「マスクが真因」仮説は否定寄りで撤回**: 接合波ピーク (軸 x/rt=1.33, ΔM=+0.115)
  の C⁻ 壁足を**実 CFD 場でトレース**すると x_w=0.154 = **凍結円弧上** (設計壁始点
  x_w0=0.2574 の上流) に着地し、設計壁のどの自由度でも原理的に到達不能。
  一方 x/rt=1.55 (足 0.259)・1.80 (足 0.420) は設計壁上で修正可能域。
  スパイクは R スイープ (上表) の Sauer 一次打切り誤差と整合 → **B の本丸
  (KL 高次解 or 遷音速 CFD 直結, or R 下限) が必要**。選択は B3 の 2×2 に従う。
- **run_0015/0016 に残っていた欠陥 3 件 (Codex レビュー) → 是正済み**:
  (i) 軸 M 定常ゲートが**配線漏れで一度も実行されていなかった** → `gate()` に
  `axis_window`/`scale` を実接続し、判定は**レート正規化** (隣接スナップ対の
  max|ΔM|/Δstep を n_ref=6000 step へ射影、判定は最新対) でスナップ間隔非依存に;
  (ii) 接合部 r'' が実際の壁表現 (not-a-knot spline) で未拘束 (+0.51/−3.21 の跳び)
  → `ModeFWall` を**左端に円弧の解析 r',r'' をクランプした 5 次 B-spline** に変更
  (テーブル全点通過は機械精度のまま、変形は最初のノット区間に局在、実測
  +0.5127/+0.5114)。撤回済み「曲率ブレンド」(幾何上書き) とは別物;
  (iii) メッシュ変更後の cold start (AGENTS の cross-mesh restart 必須に違反) →
  pass 1 のみ cold アンカー (正式 PASS)、pass≥2 は `interp_field.py` warm restart +
  **CHUNK=6000 step の適応継続** (軸 M 定常になり次第停止、上限 4 チャンク)。
  warm パスのゲートは「非 DIVERGED + 軸 M 定常」(低下桁数は構造的に満たせない
  ため要件を置換、verdict は事実のまま記録)。検証 run: `run_0017_narrow_c2rep`。

**B5. Sauer vs CFD の直接測定 (2026-08-15, run_0017/pass_08 の場・新規 CFD なし)**

レビュー指摘「(M,M′)_Sauer=(M,M′)_CFD は一度も確認していない」を受けた 3 測定。
CFD を正とすると **Cauchy データそのものが大きくずれ、特性線マップは概ね正しい**:

1. **軸上 @x0=0.2574** (CFD は窓 ±0.15/0.25/0.40 の 4 次フィットで安定):

   | | M | M′ | M″ |
   | --- | --- | --- | --- |
   | CFD (実態) | 1.0568 | 0.634 | +0.14 |
   | Sauer (設計アンカー) | 1.0500 | **0.794 (+25%)** | +0.326 (2.3×) |
   | 目標 Bézier | Sauer に一致 | Sauer に一致 | −0.483 (符号違い) |

2. **starting line (x=x0 縦線) の半径方向**: 不整合は壁に向かって増大。
   r=1.017 で ΔM=−0.088 (Sauer 1.410 / CFD 1.322)、Δθ=−0.48°。さらに壁足 θ は
   円弧接線 7.394° で上書きしているが CFD 近壁 θ≈5.4°。
3. **C⁻ マップ (凍結 MOC vs CFD 場トレース、被覆 x_w≤7)**: 着地差は中央値 0.14 /
   最大 0.24 r* と**小さい** — 帰還の対応表は概ね健全 (中域が直る実測と整合)。
   注: CFD マップは x_w>7 で C⁻ がメッシュ出口 (24.4 r*) を越えて棄却され作れない
   (= C3 の評価窓/メッシュ延長と同根)。初回比較で x_w>7 に出した「差 −20 r*」は
   np.interp の端値クランプによるアーチファクトで無効 (被覆確認を怠った)。

**解釈**: pass 1 から存在する接合波は (1)+(2) = **設計の遷音速 Cauchy データが実流
と不整合**なことが支配的で、マップ (3) は副次的。幾何 C2 (B2) で消えなかったのは
流れ場側の接続条件が残っているから (レビューの解釈に一致)。**KL 実装より先に、
CFD 抽出値をアンカーにした目標曲線 + starting line 置換 (遷音速 CFD 直結) を試す**
のが最短 — 遷音速部は下流形状にほぼ依存しない (実測 ΔM=0.0024) ので循環参照は
実務上問題にならない。

**B6. CFD アンカー化 (2026-08-15 採択 — B5 の帰結。A/B 検討の「B 案」)**

- 方式比較 (ユーザ検討): (A) 円弧切断角 φ を dv に加えたコーン延長スタブを別 CFD して
  スロート場を策定 / (B) 現行チェーンの CFD からアンカー抽出して逆設計。**遷音速場は
  下流壁にほぼ依存しない** (実測 ΔM=0.0024) ため両者のアンカー品質は等価で、
  超音速域の擾乱は下流にしか伝わらないのでコーン接続波も現行接合波も抽出線上流を
  汚染しない。→ **B を先行** (追加 CFD ゼロ・既存ループへ挿さる・仮説検証が最短)。
  A は MOO 段で φ を dv 化する/(R,φ) スロート DB をキャッシュ分離するときに昇格。
- 実装: `feedback/cfd_anchor.py` の `CFDThroat` (SauerThroat 互換 `starting_line`/
  `mach`)。軸 M は局所多項式フィット (x0 は CFD 軸 M=M_start の根)、線データは
  偶 (M)/奇 (θ) 多項式最小二乗。`geometry.throat_anchor_run` で設定し、アンカーは
  抽出元 run に**凍結**。目標 Bézier は `centerline_start_order: 2` で M″ まで
  CFD に整合させる。検証: `run_0018_cfd_anchor` (narrow 構成のループ、pass 1 の
  接合帯を run_0017 pass_01 基準 0.1176 と比較)。
- **残課題 (ユーザ指示 2026-08-15)**: **初期遷音速解の二次化 (Kliegel–Levine)**。
  CFD アンカーが機能しても、(i) ブートストラップ元 pass 1 の初期壁品質、
  (ii) CFD を回さない高速プレビュー経路、(iii) R 小の制約緩和のため残す。

**B7. 目標曲線の $x_{\mathrm{reach}}$ 分割 (2026-08-15 採択 — taper A/B 診断からの派生)**

- **経緯**: run_0019 (taper=0 vs 0.3) の A/B で x/rt=1.66/1.93 は taper 起因と
  triangulate (曲率差・壁圧 diff-of-diffs・特性線後退トレースの 3 点が
  x_w≈0.25–0.33 で一致) できた一方、**主残差 (x/rt≈1.0–1.6, ΔM 最大 +0.05) は
  taper と無関係**と判明。$M_{\mathrm{target}}(x)-M_{\mathrm{CFD}}(x)$ を
  到達不能域 ($x_0\le x<x_{\mathrm{reach}}\approx1.62$) だけに絞って直接プロット
  すると、なだらかな山型 (急峻な波ではない) で同じピークが再現され、**Bernstein
  基底の重み計算で x/rt=1.38 時点で遠方 CP2 (M=3.68、ジグザグの起点) が既に 11%
  の重みを持つ**ことを確認。目標曲線のグローバル台が主残差の主因と結論。
- **設計**: $x_{\mathrm{reach}}$ (= 設計壁始点 $x_0$ から出て**最初に成功する**
  C⁻ の軸着地点。`build_cminus_map` の被覆下限、taper 幅とは無関係の幾何量) を
  境に目標を分割する。
  $$M_{\mathrm{target}}(x)=\begin{cases}M_{\mathrm{CFD/Sauer}}(x), & x_0\le x<x_{\mathrm{reach}}\\
  M_{\mathrm{design}}(x)\ (\text{自由 CP Bézier}), & x_{\mathrm{reach}}\le x\le x_d\end{cases}$$
  $x_{\mathrm{reach}}$ で $M,M',M''$ を接続 (Bézier の始端拘束と同じ機構で C2 厳密)。
- **実装**: `design_chain` を 2 段ブートストラップ化。① $x_0$ アンカーのみの旧来
  単一 Bézier で仮設計 → `build_cminus_map` の被覆下限から $x_{\mathrm{reach}}$
  を得る (MOC のみ、CFD 不要・安価)。② 実目標を構築し直し、本番 MOC を再実行。
  **区間全体を 1 本の曲線として直接フィットする案 (`axis_segment`, bin 平均・
  高次多項式・等調回帰) は実装したが不採用**: この近スロート域は cell モードに
  軸上 DOF が無く軸方向のセル列が疎 (幅 1.4 $r_t$ に ~30 列) で、6 次多項式は
  Runge 振動で 27 点非単調、bin 平均 (10〜60 bin いずれも) は列疎密混入で非単調、
  等調回帰は非単調区間を丸ごと平坦化し $x_{\mathrm{reach}}$ で $M'\approx0$ に
  (物理的にありえない)。**最終形**: $x_0$ の点アンカーと同じ狭窓ローカルフィット
  を $x_{\mathrm{reach}}$ でも取る `CFDThroat.local_anchor()` (ただしセル疎な
  一般点向けに $x_0$ 用より低次・広窓: degree=2/halfwidth=0.4 — degree=4/
  halfwidth=0.15 だと $x_{\mathrm{reach}}$ 近傍のセル不足で $M'$ が負に反転した)
  で両端の $(M,M',M'')$ を得て、区間全体はその 6 条件だけで決まる**5 次 Hermite**
  (free_cp なしの `MachBezier.from_constraints`) で繋ぐ。Sauer 時は
  `SauerThroat.mach()` (解析式、全域で有効) をそのまま使う。
  **検証** (2026-08-15, `problem_m4_cfdanchor.yaml`): target 単調 (符号反転 0)、
  `wall.validate()` 合格、mdot 比 0.993 (旧構成の 0.844→0.993 に改善)、
  リップ M=3.996。実 CFD 単発検証は run_0020_b7_reach で実施中。
- **B7 の CFD 検証で 2 案が失敗 → 最終形 = 設計/評価目標の分離 (2026-08-15)**:
  1. **run_0020 (両端 Hermite)**: 接合帯 0.0729 と B6 (0.0498) より悪化。原因 =
     到達不能域には**接合波の軸着地による実在のこぶ** (x/rt≈1.4、帯規約実測で
     M 1.83 まで上がり x_reach で 1.80 へ戻る、3 スナップ平均で再現) があり、
     両端だけ合わせた滑らかな Hermite はこれを潰す → 評価が実流と食い違い
     人工残差を計上。
  2. **run_0021 (こぶを目標に写す)**: ΔM 1.84 と壊滅。こぶは「円弧が作る波の
     結果」なので逆設計の目標に入れると設計壁が同じ波をもう一度作る (**原因の
     二重計上**)。さらに M 減少区間の Cauchy データは特性線を収束させ逆設計が
     悪条件化 (壁 d2θ 9° [通常 0.3°]・mdot 0.87→0.90)。
     **測定規約の教訓も併記**: ΔM 評価 (`axis_mach`) は r<band セルをそのまま
     使う帯規約で、近スロートでは真の軸値 (r→0 偶基底外挿) より +0.02〜0.04
     大きい。目標と測定は同一規約でなければ系統残差になる —
     `axis_segment_curve` は帯規約 (列ごと r<band 平均) で抽出する。
  3. **最終形**: **設計用と評価用の目標を分離**。設計 (inverse_design 入力) =
     滑らかな単調データ (x0↔x_reach Hermite [x_reach アンカーは単調な
     `local_anchor`] + 自由 CP Bézier)。評価 (ΔM 測定) = 到達不能域は実測こぶ
     曲線 (帯規約 C2 平滑化スプライン)・以降は設計 Bézier。x_reach の設計 vs
     実測の段差 ~0.02 は「x_reach 以降に届く波の残余 = 帰還が打ち消すべき量」
     として意図的に評価目標へ残す。検証 run_0022_b7_split。
- **$M_{\mathrm{start}}$ の扱い**: $x_0$ を決める $M(x_0)=M_{\mathrm{start}}$
  (既定 1.05) はこれまでハードコードだったが、物理定数ではなく数値上の選択
  なので `geometry.M_start` として YAML 化する (ユーザ指示 — 1.03/1.05/1.08 で
  $x_{\mathrm{reach}}$ と設計結果の不変性を確認できるようにする)。
- **注記**: 補正壁を接合直後で実質動かせなくする正則化 (B4/taper の後継、未着手)
  を入れる場合、そのマスク/reach 起点は本節の幾何学的 $x_{\mathrm{reach}}$
  ではなく「taper 幅込みで最初に十分な感度を持つ壁点」を別途使うべき (taper=0
  なら一致するが、有限幅 taper ではより下流になる) — 混同しないこと。
- **検証**: run_0018 相当の単発評価で $x_0\le x<x_{\mathrm{reach}}$ の $\Delta M$
  が崩壊することを確認してから、8 パスループを回し直す。

**B8. 設計=B6 グローバル / 評価=B7 分割帳簿 / アンカー=B6 場 (2026-08-16 採択)**

- **B7 の物理後退の発見** (ユーザ質問「結局スロート近くの M は良くなったのか」が
  契機): 意図非依存のうねり指標 (実流軸 M(x) の 3 次カーブからの片振幅,
  x/rt 0.55–2.5) で全世代を測ると **Sauer ±0.073 → B6 ±0.023 → B7 ±0.062**。
  B7 は帳簿 (到達不能域 ±0.009) が良く見えたが、参照アンカーが Sauer 壁の
  波だらけの場 (run_0017) だったため実流の後退を隠していた。B6 のグローバル
  Bézier は逆設計を通じて実質的に接合波をキャンセルする壁を作っていた。
- **B8 構成**: 設計目標 = B6 グローバル Bézier (x0 アンカー・自由 CP・[x0,xd]
  一本、ブートストラップ=最終設計で逆設計は 1 回に短縮) / 評価目標 = B7 分割
  (到達不能域 [x0,x_reach) は実測こぶ曲線・以降は設計 Bézier) / **アンカー
  抽出元を run_0018/pass_08 (B6 壁の場) に更新** (`problem_m4_b8.yaml`)。
  x_reach マスク境界・被覆外除外・規約整合抽出などの B7 資産は全て維持。
- **結果 (run_0026_b8) — プロジェクト初の正式収束**: pass 9 で
  **converged: true** (masked ‖ΔM‖∞ = 0.0198 = **0.49% Md ≤ tol 0.5%**)。
  90k step・ゲート中止なし・masked は 0.0550 から単調降下。
  **物理うねり ±0.0242 ≒ B6 (±0.0232)** = スロート近傍の実流品質を維持した
  まま正直な帳簿と収束を両立。帯別 (pass_09): 接合帯 0.0198 / 中域 0.0191 /
  全域 0.0229。①モード F の Euler 帰還 (v2 凍結マップ) はこれで基準達成 —
  次段は NS/δ* (v1→v2 の粘性段) か MOO 統合。

**B9. 接合波の最終診断と「実用上の決着」 (2026-08-16, ユーザ問い合わせ)**

- **問い「つなぎ目の解は計算開始当初から変わっているのか」→ 否**。cold pass の
  時系列で、こぶ振幅は step 0 (準 1D IC) で 0.0005 → step 6000 で 0.0263 →
  **step 12000/18000/24000 で 0.0263 と 4 桁完全一致**。初期数千 step で形成後は
  完全に凍結した**定常構造**であり、過渡でも数値ノイズでもない。パス間でも
  0.025–0.026 で不動 (帰還が届かない領域だから — B5/B7 の結論と整合)。
- **波源の正体**: 壁 M の勾配が接合点で折れる (CFD 実測 `dM/dx`: 上流の円弧側
  **0.989** → 下流の設計壁側 **0.595**、40% の跳び)。r,r',r'' を C2 連続化しても
  残るのは、上流側が**遷音速加速** (2D 構造が支配、局所壁曲率では決まらない) で
  下流側が**超音速 PM 膨張**という、支配方程式そのものの切り替わりだから。
  加えて starting line 壁端の Cauchy データに残差: M は MOC 入力 1.3739 vs
  CFD 実測 1.3586 (**1.1% 過大** — 偶多項式フィットの壁端外挿オーバーシュート)。
- **実用上の決着 — テスト部には影響しない**: 出口一様性 (`core_mode="traced"`)
  | 設計 | $\varepsilon_M$ | $\|\theta\|_{max}$ |
  | --- | --- | --- |
  | Sauer (run_0017) | 0.0567% | 0.0812° |
  | **B8+C3 (run_0027)** | **0.0319%** | **0.0181°** |
  | 風洞要求 | < 0.5% | < 0.1–0.25° |
  → **要求に対し $\varepsilon_M$ で 15 倍・$\varepsilon_\theta$ で 5–14 倍のマージン**。
  接合波は x/rt≈1.4 の上流側にあり、テストコアに達するまでに完全に洗い流される
  (`exit_profile_comparison.png`)。**接合波は視覚的には目立つが機能要求上は無害**で、
  一連の改良で出口指標はむしろ Sauer 比 44%/4.5 倍改善している。
- **なお詰めるなら** (優先度低): (i) starting line 壁端の抽出改善 (外挿を止め
  壁 BC の θ と整合させる)、(ii) $R$ を増やす (R スイープで遷音速不整合 ∝ ū ∝
  $1/\sqrt{R}$ を確認済み。R は dv なので MOO が選べる)、(iii) 遷音速解の二次化
  (KL、既に残課題)。**いずれも εM 0.03% を更に下げる価値は乏しく、現状で
  Phase 3 の①要求は達成済みと判断してよい**。

**B10. x_reach_CFD 起点 target への方針変更 (2026-08-17, ユーザ指示 — 正本)**

**B9 の「支配方程式切替による不可避な無害波」という結論は撤回する** (レビュー
指摘)。CFD は全域で同じ Euler を解いており、切り替わるのは**設計モデル**
(CFD 遷音速パッチ→MOC/PM) であって物理ではない。接合両側とも局所 M≈1.35 で
超音速なので、壁 $dM/dx$ の折れ (0.989→0.595) は不可避な物理ではなく**接続
データ/設計モデルの不整合**を疑うべき。また壁 M は境界条件でなく観測量なので、
折れを示しただけでは因果が逆。starting line 壁端の 1.1% 不整合 ($M$ 1.3739 vs
1.3586、$\Delta M$=0.015) は軸こぶ振幅 0.026 に対し無視できず、**最有力候補**。
「洗い流される」も Euler では不適切 (散逸なし — 特性線の広がり・波の相殺・
数値散逸の結果であり、MOO で条件が動けば同じ相殺は保証されない)。

**新方針**: 設計 target と自由設計変数の起点を、$x_0$ ではなく
**$x_{reach,CFD}$** (自由壁が軸へ影響できる最初の位置) にする。

- **用語** (混同禁止):
  - $J=(x_j,r_j)$: 壁上の**固定円弧終端 = 可変壁開始点** (実測 0.2465, 1.0153)
  - $x_{reach,CFD}$: **$J$ から出る CFD C⁻ 特性線の軸着地点**
  - $x_0$: 従来の $M_{start}=1.05$ の縦 starting line。遷音速データ抽出に
    内部的には残すが、**新 target・自由 dv の起点には使わない**
  - $J$ と $x_{reach}$ は同じ $x$ ではない
- **フロー**: ①現生産基準壁を仮壁とする ②仮壁の全ノズル CFD 場から $J$ 発の
  C⁻ を追跡 ③軸着地点を $x_{reach,CFD}$ として**固定** ④そこで CFD の
  $M,M',M''$ を取得 ⑤$[x_{reach,CFD}, x_d]$ に target と自由 CP を定義
  ⑥仮壁を初期値に既存 CFD 帰還で $J\to$リップを更新 ⑦$J$ の $r,r',r''$ を
  全パスで保存
- **$x_{reach,CFD}$ の取得**: 逆 MOC 場の `build_cminus_map()` は**新正本では
  使わない**。実 CFD 場の $\theta$ と $\mu=\arcsin(1/M)$ で $J$ から軸まで
  積分する。既存 `build_cminus_map_cfd()` は診断用実装 (最新 1 スナップ・
  壁 0.985 倍から出発・cell 中心補間・軸近傍直線外挿) なので**無検証で正本化
  しない**。記録必須の感度: 末尾複数スナップ平均 / 出発オフセット依存 /
  壁面への外挿法 / 軸近傍外挿誤差 / 積分刻み依存 / MOC 由来との差 /
  下流壁を変えたときの不変性。基準 CFD が正式収束根拠を満たさない場合は
  **現最終壁で新しい cold 基準 run を作り正式ゲートを通す** (既存 run の
  再利用・上書き禁止)。$x_{reach,CFD}$ は基準 run から一度取得しループ中は固定。
- **target**: $M_{target}(x)$ を $x_{reach,CFD}\le x\le x_d$ に**のみ**定義。
  $x<x_{reach,CFD}$ には target を置かず帰還・収束判定から除外する。
  **旧「CFD 実測こぶ曲線を上流評価 target へコピー」する帳簿処理は新系列で
  使わない** — 上流の軸 M・こぶ振幅・圧力勾配は target 誤差とは別の
  **物理診断**として記録する。$x_{reach,CFD}$ で $M,M',M''$ を CFD から継承。
  $M''$ はノイズ敏感なので局所フィットの次数・窓幅・格子依存を確認し、単一の
  任意フィット値を確定値にしない。軸 M の抽出規約 (真の軸値 / 近軸帯平均 /
  node vs cell) を明示し感度を記録する。
- **自由 CP**: 初回検証では**3 個のまま**。旧 $x_0$ 起点用の
  `mc_cp1/2/3 = 2.0, 2.9, 3.6` を**インデックスずらしで流用しない**。初期値は
  $x_{reach,CFD}$ の C2 拘束と $x_d$ の $(M_d,0,0)$ 拘束の下で現行下流 target
  との差を最小化する**拘束付き最小二乗で決定的に射影** (手調整禁止)。制御
  多角形の単調性と実曲線の単調性を混同せず $M,M',M''$ を実際に検査する。
  **今回の A/B に CP 手調整・CP 数変更・KL・R 変更・δ\* 修正・taper 変更を
  混ぜない**。
- **帰還**: 仮壁を初期値。$x_{reach,CFD}$ 固定。$x<x_{reach,CFD}$ の残差は
  帰還に入れない。target 起点をパス毎に動かさない。残差→壁マップは必要なら
  各パス CFD 場から更新可。可変壁は $J\to$リップ。$J$ の $r,r',r''$ を毎パス
  実メッシュ壁で検査。finite-width taper は現行条件固定 (必要性は別 A/B)。
- **比較・判定**: 旧系列との比較で `target_eval` の切替 seam を物理波と呼ばない。
  共通の物理量で比較する: 軸 M そのもの / 滑らかな共通基準からの上流こぶ・
  うねり振幅 / 軸・壁面の圧力勾配 / $x_{reach,CFD}$ 以降の target 誤差 /
  接合付近の波の位置と振幅 / 出口 $\varepsilon_M$ / 出口 $|\theta|_{max}$ /
  帰還の単調性と最終壁変位。**成果物**: ①$J$ から軸までの CFD C⁻ 特性線図
  ②$x_{reach,CFD}$ vs 従来 $x_{reach,MOC}$ ③$x_{reach,CFD}$ での $M,M',M''$ と
  抽出感度 ④新旧 target と自由 CP の図 ⑤帰還 pass 毎の軸 M・target 誤差
  ⑥上流こぶの独立診断 ⑦接合 $r,r',r''$ の検査 ⑧出口指標と準定常判定。
- **run 計画**: `run_0031_baseline_cold` (仮壁 cold 基準・正式ゲート →
  $x_{reach,CFD}$ 取得) → `run_0032_xreach_loop` (新 target + 既存帰還、
  仮壁初期値、12 パス)。対照は `run_0027_c3` (旧系列)。

**C. 帰還ループの正しさ**
- C1 v3 の reject 時に**特性線マップも巻き戻す** (best にマップを保存。現状は棄却場のマップで最良場の残差を戻している)
- C2 **node 対応**: `discretization` に応じて座標配列 (CELLS/NODES) を選ぶ
- C3 評価窓を $x_d$ より下流へ延長 (壁の 81% が無修正の帯を解消 — §9 2026-08-14 の項)。
  **着手 (2026-08-16, NS/δ* 段の前提としてユーザ指示)**: 評価窓・出口ランプ・
  軸 M 定常ゲート窓をリップ−マージンまで延長 ($x_d$ 以降の目標 = $M_d$ 一定は
  target が既に返す)。共通指標に下流帯 `dM_inf_downstream_band` を追加。
  注: メッシュ出口 (≈リップ) より下流に軸点は取れないため、帰還が届く壁は
  「C⁻ 足がメッシュ内に着地する範囲」(x_w ≲ 13) まで — それよりリップ側の壁は
  最終 Δθ の定数外挿。リップ近傍壁の C⁻ は出口面より下流に着地するので
  テストコア一様性への影響は限定的 (有効菱形の外)。
  **masked 指標の意味が変わる**ため run_0027 以降の数値は run_0026 以前と直接
  比較しない (帯別指標で比較する)。
  **結果 (run_0027_c3, 2026-08-16)**: 下流帯 0.0359→**0.0090** (4 倍改善 —
  無修正帯の解消を実証)・中域 0.0018・物理うねり ±0.0231 (B6 最良同等)。
  masked 0.0259 は **x=1.62 (x_reach 境界) の単一ステーション**が律速 —
  seam の意図的段差 (+0.03) とテーパ帯壁足の既知制約の複合で、直せる全域は
  実質収束。この残余を詰めるなら「拘束付き滑らか補正基底」(レビュー提案の
  taper 後継) が次の手。C3 は完了とする。
- **NS/δ* 段の前提 (2026-08-16 ユーザ指示)**: node の壁関数経路は既知の
  Cf 欠損があり信用しない ([[node-wallfunction-pk-convention-deficit]]) —
  **壁解像 y+~1 の低 Re SST (node)** で行う。壁解像 node は SU2 一致で健全
  (0.943 vs 0.938–0.947)。
- **NS v1 完了 (2026-08-16, run_0030_ns_v1)**: δ* 相関 (Edenfield 系,
  `feedback/deltastar.py`) オフセット壁 + y+1.5 node 低 Re SST。
  - **結果 A (軸 M)**: 無帰還で全域 |ΔM|≤0.047 (1.2% Md)。中域 0.041・テスト部
    (14–20) に 3.968→4.006 の勾配 — v2 NS 帰還で直す残差として小さく良形。
  - **結果 B (δ* 実測/相関比) — 訂正 (2026-08-16, ユーザ指摘)**: 初回測定
    (壁法線探索窓 0.35 r*) は**誤り**。窓がフリーストリーム到達前に切れており、
    境界層が厚い下流ほど δ* を過小評価する系統誤差があった (x=16–20 で
    測定 919→876 µm と**物理的に不自然な減少**を示していたのが手掛かり)。
    探索窓を 1.5 r* へ広げて再測定すると **x=8–22 の信頼域で比は一貫して
    1.04–1.09** (相関はむしろ 4–9% 過小評価、下流ほど微増) と、初回の
    「整流区間で相関が過大」から**符号も大きさも逆転**した。x<8 は測定不能
    (スロート近傍はコア流が半径方向に未一様化しており「ρu 極大=境界層縁」の
    単純判定が破綻、BL 厚と称する値が非物理的に巨大 [17–20 mm] になる)。
    → **相関は全信頼域で妥当**。v2 (実測 δ* での相関置換) の期待効果は
    当初想定より小さい — 優先度は下げてよい。
  - **起動レシピ (M4・低圧・y+1.5 固有)**: Euler 場+一様 k/ω からの直接起動は
    3 法とも出口∩壁コーナー ω NaN (run_0028)。**必須チェーン** = 粗壁 (5e-3)
    SST 中継 (run_0029, ALL PASS) → interp + **ω 底層フロア** $6\nu/(\beta_1 y^2)$
    (最近傍 interp は細メッシュ近壁 ω が 5 桁不足) → soft(1次 cfl0.5)
    → mid(2次 cfl1) → **本段 cfl1** (cfl2 は step176・cfl4 は step40 で
    コーナー NaN — 出口静圧 6.6 kPa × y1 1.5 µm の ω~1e12 剛性)。
    背圧は M4 整合圧 6588 Pa 必須 (Euler の 1000 Pa 流用は NS では
    出口 BL が読んで吸い出し発散)。

**D. 記録の同期** — plan とコード docstring を巻き戻し後の状態に合わせる (本節で plan は完了、コード側は A と同時に)。§10 の 2026-08-14 項も撤回注記済み

## 10. 変更ログ

- `2026-08-15` — **B7 完結 (run_0022〜0025) + パイプライン 3 バグ修正**。§9.2 B7 の
  最終形 (設計/評価目標分離) を 8 パスループで検証完了:
  - run_0025: 8 パス完走・ゲート中止なし・90k step。masked 0.0585→**0.0367
    (0.92% Md)** 単調改善。到達不能域の帳簿残差 (B6 の +0.05) は ±0.009 に解消。
    残る境界スパイク 0.062 は「凍結アンカー場と現在の壁の波位置ズレ」の診断値
    (w=0・帰還対象外)。
  - ループ検証で発見・修正した 3 バグ: (i) 被覆外クランプで到達不能境界が w=1 に
    化け誤帰還 (run_0023)、(ii) x_reach (ブートストラップマップ) と ax2w (最終
    設計マップ) の被覆差の隙間ステーション混入で masked 頭打ち (run_0024)、
    (iii) `check_convergence.py` の rising 判定がプラトー呼吸 (±5%) を発散と誤読
    — 「系列最小の 2 倍超」ガードを追加 (3.4 桁収束した run が非決定性で
    PASS/NOT CONVERGED に割れる実害。本物のリバウンドは依然検出)。
  - 壁更新後の遅い波 (trend 0.008 vs tol 0.005) が 2 run で再現 →
    MAX_WARM_CHUNKS 4→6 (36k step まで許可)。
  - 残課題: masked 0.037 は tol 0.02 に未達 (軌跡は単調降下・ω 半減で減速)。
    出口側/中域 ~0.03 は設計 Bézier の形 (CP 再チューニング保留中) が関与の疑い。
- `2026-08-15` — **B6 CFD アンカー実装・検証完了 (run_0018_cfd_anchor) — 仮説確定**。
  `CFDThroat` (feedback/cfd_anchor.py) + `geometry.throat_anchor_run` +
  `centerline_start_order: 2`。抽出品質: 線フィット rms M 0.0014 / θ 0.035°、
  壁足 θ 外挿 6.98° vs 円弧接線 7.08° (Sauer は 5.89° で 1.5° 差 → 0.1° 差に)。
  結果 (run_0017 同構成比):
  pass1 接合帯/中域 = Sauer 0.1176/0.1160 → **CFD 0.0498/0.0295**、
  pass8 接合帯/中域 = 0.1152/0.0365 → **0.0520/0.0166**、
  pass8 masked = 0.0664 → **0.0223 (0.56% Md)**。
  **帰還ゼロの pass 1 で接合波 58% 減 → Cauchy データ不整合が主因と確定** (B5 の
  解釈どおり)。残存 0.052 は同位置 (x/rt≈1.4)・同波形の半減で、候補は抽出平滑化・
  cell モード近壁データ・縦線 Cauchy が持てない 2D 遷音速構造・MOC 伝播誤差。
  次の絞り込みは (i) パス追加で masked 収束 (僅差 0.0223 vs 0.02)、(ii) KL 二次
  遷音速解 (残課題として明記済み — 初期壁品質/CFD 不要経路/低 R)、(iii) node 化
  (軸・壁上 DOF で抽出品質向上)。
- `2026-08-15` — **B4 A/B 完了 (run_0015/0016) + Codex レビュー 4 点是正** (§9.2 B4
  の結果の項参照)。要点: narrow マスクは中域を 2 倍改善するが接合波ピークは不変。
  ピークの C⁻ 壁足は凍結円弧上で設計壁から不可達 (「マスクが真因」は撤回)。是正 =
  接合 C2 を壁表現で拘束 (clamped quintic) / 共通マスクなし指標 / 軸 M 定常ゲート
  実接続 (レート正規化) / warm restart + 適応停止 (無駄ステップ削減、cold 24000→
  warm 6000〜)。検証 `run_0017_narrow_c2rep` 投入。
- `2026-08-15` — **run_0017 完走 + 軸 M 定常判定の最終形**。
  - **判定器は 2 回作り直した** (経緯を残す): (1) 最新スナップ対のレート射影 →
    中域 x/rt≈9–10 の**定在音響リミットサイクル** (片振幅 ~0.003、24000 step で
    平均一定を実測) の位相サンプリングノイズ (対差 0.0012–0.0053) を DRIFTING と
    誤報。(2) 窓の点別**線形トレンド** (振動を平均化) + 平均まわり振幅 → 振幅
    0.0059>0.005 の僅差で中止したが、リミットサイクルは回しても振幅が縮まないため
    振幅ゲートは原理的に不適。(3) **最終形 = トレンド (系統移動) < tol かつ
    OSCILLATING は平均の標準誤差 amp/√n ≤ tol** — 測定は末尾 5 スナップ平均なので
    守るべきは平均の精度。超過時は継続チャンクでサンプルを増やして締める
    (pass_06 で実働: 6000→12000 step で合格)。
  - **結果**: 8 パス 90k step (旧 192k、誤報浪費を除く定常状態では ~66k)。
    中域 0.1160→0.0365 (run_0016 0.0353 と同等)。**接合帯 0.1176→0.1152 =
    run_0016 (0.1154) と同一 — 接合 C2 の表現化は接合波を消さない**。
    B2 (幾何 C2) 単独では不十分の実証 = Sauer 誤差説をさらに補強。
    → 次は B の本丸選択 (KL 高次解 / 遷音速 CFD / R 下限) を B3 の 2×2 で。
- `2026-08-14` — **v2 帰還の被覆ギャップを発見** (ユーザ質問「軸長より下流から特性線を逆走
  しているか」から判明。実装は壁→軸の**前進**積分で表を作り帰還時に逆引き、逆走積分はしない):
  - C⁻ の軸着地は常に下流で、ずれはスロート付近 +1.4 → リップ付近 **+12.8** と拡大する。
    現行の評価窓は軸 $x_i \le x_d-0.2$ で切れているため、**帰還が触れる壁は $x_w=1.07$–$4.76$ =
    全長の 19% のみ。残る 81% (整流・波キャンセル担当区間) は無修正**。
  - **実害は現時点でなし**と実測確認: 無修正区間の軸目標は「$M=M_d$ 一定」の自明データで
    v1 逆設計が既に高精度 → 一様域の軸偏差 0.22% $M_d$、**リップ断面で $\varepsilon_M$=0.032%
    ・$|\theta|_{max}$=0.037°** (風洞要求 0.5%/0.1–0.25° を十分満たす)。
  - **要対処 (ステップ 5b として起票)**: (a) 検証されていない (今回良かっただけ)、
    (b) **NS 段では δ\* 補正が全長にかかる**ため整流区間の誤差も帰還対象になる。
    → **評価窓を $x_d$ より下流 (目標 = $M_d$ 一定) まで延長**し、無修正帯を解消する。
    併せて $\varepsilon_M/\varepsilon_\theta$ をメトリクス実装 (ステップ 4 の残り) に昇格。
- `2026-08-14` — **【この項の結論は §9.1 で全面撤回済み — 以下は経緯の記録】**
  スロート直後圧縮波の根本原因調査 (ユーザ指摘「二階微分がスムーズでない/
  最適化対象外に問題」+「$M''$ 無効の知見を疑え」が的中):
  1. **誤診の訂正**: 「目標側 $M''$ を合わせても壁曲率に効かない」は誤診だった。真因 (第一) =
     `fill()` の単位プロセス役割 (C⁺/C⁻ 担体) を前線リスト順に固定していたため、**縦の
     starting line 区間で幾何的に逆転**し、交点が後方に出て黙って棄却 → **スロート直後の
     楔領域が無計算**のまま壁流線が凸包補間で通過していた (壁足曲率 −3.19 のゴミ)。
     → 両割当てを試し前方交点を採る向き非依存 fill に修正 (**→ 撤回**: 楔が誤データで埋まり出口指標が悪化。§9.1)。単体 ALL PASS 維持、
     出口半径精度 0.9964→**0.9986** に改善、壁足曲率ゴミ消滅 (−3.19→+0.03)。
  2. **第二の構造限界**: 修正後も壁足曲率は +0.03 で円弧の 0.51 に届かない。レベル充填は
     前線が 1 点/レベルで縮む構造のため **starting line 頂点の C⁻ より上の「壁帯」を
     原理的に埋められない** (壁帯は壁自身の依存領域)。古典の正解 = **C⁺ 線マーチ +
     壁点の質量流束閉包** (Foelsch/CONTUR 型 — C⁻ 担体不要)。`_CPlusMarch` として実装着手、
     はしご構造まで到達したが壁閉包の凍結延長が粗く壁角が崩れる — **WIP** (配線は旧経路に
     戻して ALL PASS 状態で退避)。
  3. **暫定対策 (→ 撤回済み。§9.1)**: `ModeFWall` に円弧→設計壁の**曲率ブレンド (5 次 Hermite, C2)** を実装
     (収縮→円弧接続と同じ手法)。blend_len スイープ: 0.4 で振れ幅 −0.47、1.2 で −0.20
     (→ **ブレンド自体を撤去**。C2 は B1/B2 で保証する — §9.2)。
  4. 可視化を納品: `case/41/contour_initial_vs_final.png` (v2 の補正は −51μm@x37mm の
     ディップ + 尾部 +20μm — 総補正 ~50μm/rt10mm で 2.5%Md を直した)、
     `characteristics_net.png` (C+/C− 網・starting line・壁)。再生成 = `viz_contour_chars.py`。
- `2026-08-13` (夜間自走) — **§5 ステップ 1–5 完了 (v1+v2 成立)**:
  - **逆 MOC マーチ** (`moc_inverse.py`): 軸 Cauchy 三角充填 + 壁流線抽出。単体 ALL PASS
    (一様流=機械精度 / 源流レイ 0.13% / Md4 ノズル: リップ完成・$r_e/\sqrt\varepsilon$=0.996
    [=√Cd]・質量保存 0.2%・決定性)。軸目標の下流延長則を定量確認 (リップ回復には
    C⁻ 足まで = 素朴 margin の約 2 倍)。
  - **v1 チェーン** (`wall_modef`/`deltastar`/`runner_wt`, `case/41` run_0001): Md=4 設計壁の
    forge Euler 実測 = 出口軸 M 4.024 (+0.6%)・ΔM_max 3.0%Md。誤差構造 = 接合波の軸着地
    スパイク (円弧/設計壁 C1 接合の曲率跳び — マスク領域) + 減速部の滑らかな欠損 (v2 対象)。
    Sauer 線形化の壁 θ 過小は kernel と同じ接線上書きで解消。
  - **v2 Euler 帰還** (`euler_loop.py`, run_0003_v2_tr): 凍結 C⁻ マップ + PM 換算 + 平滑化/
    クリップ + 同一トポロジ再メッシュ + warm restart。固定 ω=0.4 は高 M 末端でリンギング
    (run_0002 — dν/dM 縮小による実効過ゲイン) → **決定的 trust-region (悪化 reject → 最良壁
    巻き戻し + ω 半減) で単調降下 2.89%→**0.45% Md** (13 パス, reject 1回) — §4.7(c) の
    0.5% 基準達成**。図 = `run_0003_v2_tr/v2_convergence.png`。
  - 残り: §5 ステップ 4 の $\varepsilon_M/\varepsilon_\theta$ 実装、ステップ 6 (独立目標での
    自己一貫 + Sivells 照合)、ステップ 7–8 (MOO / 凝縮メニュー)。
- `2026-08-13` — 起票 (親計画 §5 Phase 3。Phase 1 の帰還エンジンを統合)。
