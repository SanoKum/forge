# forge 実装計画インデックス

`.github/plans/` 配下の実装計画 (`.md`) の一覧と状態。
新規 plan を追加・状態を変えた場合は、本表を必ず同期させる。

計画の作り方は [`_template.md`](_template.md) を起点に、命名規約 `<area>-<short-slug>.md`
(長期テーマは `<area>-<theme>/README.md`) で配置する。
運用フロー全体は [`../../AGENTS.md`](../../AGENTS.md) の
「## 開発フロー」節を参照。

## 一覧

| Plan | area | status | related_docs | 概要 |
| --- | --- | --- | --- | --- |
| [turbulence-sst-freestream-init.md](turbulence-sst-freestream-init.md) | turbulence | done | [`docs/turbulence/`](../../docs/turbulence/) | **SST 初期乱流の freestream 初期化** (`kInit`/`omegaInit`)。IC に roK/roOmega が無いと 0 初期化され ω=0 で SST が cold start 発散 (特に node wt=1)。config で freestream 値を与え回避。既定 0 でビット不変。NACA y+~50 で node SST wt=1 が安定し CL 0.78 (妥当) を出す |
| [boundary-outlet-characteristic-backflow.md](boundary-outlet-characteristic-backflow.md) | boundary | done | [`docs/boundary/`](../../docs/boundary/) | **静圧固定流出 BC を特性ベース化し逆流を静圧アンカーに統一**。旧 backflow 分岐 (全圧 stagnation 流入 + 歪速度) が剥離 BL 出口到達ケース (擬似衝撃ダクト, 壁∩出口コーナー) で過加圧→発散させていた。SU2 `BC_Outlet` 同型 ($P_{exit}$ + 内部エントロピー + 外向き Riemann, $V_n<0$ 許容) に統一し層流コーナー発散解消 (`run_0071` 生存) ・SST Ps1.90 回帰無害 (`run_0072`=`run_0067` 一致)。逆流診断は case36 で実証 |
| [turbulence-des-wmles-survey.md](turbulence-des-wmles-survey.md) | turbulence | draft（調査専用） | [`docs/turbulence/`](../../docs/turbulence/) | **DES / WMLES 最新動向サーベイ**（2020–2025, 94 エージェント deep-research）。超音速ノズル・ピントルバルブ向け手法選定。確認知見: DDES 設計意図・IDDES-SST+WENO が SBLI 主流・WMLES 平衡壁モデルは高 Re で失敗・ML 壁モデルは亜音速限定。万能な最良手法なし |
| [turbulence-des-flux-survey.md](turbulence-des-flux-survey.md) | turbulence | draft（調査専用） | [`docs/turbulence/`](../../docs/turbulence/), [`docs/convection/`](../../docs/convection/) | **DES/DDES/IDDES 用 低散逸対流 flux 設計 技術調査**（98 エージェント deep-research / SU2 master ソース + Travin/DEShybrid/Ducros 一次文献, 25 claim 検証 24 確認）。正準形は `weight=(1-σ)w₁+σw₂` の中心↔風上連続ブレンドで、**係数は乱流解像物理（f_d/NTS）駆動＝衝撃センサと直交**。SU2 DES 既定は `ROE_LOW_DISSIPATION=FD`（σ_FD=max(0.05,1-f_d), ROE・SLAU(2) 限定）＝**forge の `1-f_d` 注入と一致**。**MUSCL limiter を衝撃センサに流用する一次文献は皆無**（SU2 は実衝撃に専用 Ducros を `FD_DUCROS`/`NTS_DUCROS` で層化）。結論を [turbulence-iddes-sst.md](turbulence-iddes-sst.md) §4.8 に反映: 短期=f_d 主導・ψ は positivity フロア / 中期=改良 Ducros を OR ブレンド追加 / 長期=LD2 勾配再構成 |
| [turbulence-node-sst-wallfunction.md](turbulence-node-sst-wallfunction.md) | turbulence | blocked (SU2調査待ち) | [`docs/turbulence/`](../../docs/turbulence/) | **node の SST 壁関数 (`wallTreatmentSST=1`) が ic=壁ノード u=0 を参照し utau/ypls/wf_pk=0 に退化** (twall≡0 と同一トラップ; 実測 nonzero frac 0)。case36 で近壁 μt 異常→BL 厚化→超音速コア加速失敗→擬似衝撃波 80mm 上流 (node 46 vs cell142/SU2 132)。修正は Ut/rho/nu/y を内部代表点から一貫取得 + 複数エッジ代表点選択 + wf_pk overwrite 対策。SU2 vertex 壁関数調査の結果待ちで保留 |
| [turbulence-iddes-sst.md](turbulence-iddes-sst.md) | turbulence | in_progress | [`docs/turbulence/`](../../docs/turbulence/) | SST に DDES / IDDES の length scale 修正を追加。**Phase 1 (DDES) 完了**: `DESmode` + f_d シールド + l_DDES（k 消滅項切替, $l_\mathrm{RANS}=\sqrt{k}/(\beta^{*}\omega)$）+ 診断出力。`DESmode:0` は既存 SST とビット不変 (atomicAdd 床内, `run_0019`)、mode1 は付着 BL 保護 (`roUx` relL2 5.7e-7, `run_0020/0022`)、3D backstep 857k で剥離 f_d→1・付着 f_d→0・NaN なし (`run_0035`)。Phase 1.5 (DES 用低散逸 flux) / Phase 2 (IDDES) は次セッション。検証: case/26.flat_plate_sst + case/18.backstep |
| [architecture-perphase-profiling-hotspot.md](architecture-perphase-profiling-hotspot.md) | architecture | draft | [`docs/architecture/`](../../docs/architecture/overview.md) | 残差モニタ後も残る host 律速 (~40%, wall 4.79 vs GPU busy 2.87ms/step) を**盲目的 sync 撤廃でなく phase/カーネル単位の詳細計測で局在化**して個別最適化。初期計測で **setDT が過大** (cudaEvent 0.40ms×2/step ≫ ncu 純カーネル 0.11ms = `thrust::max_element` 同期+2重呼び出し) と判明。RuntimeProfiler 細分化→ncu 突合で overhead=event−kernel を箇所別に定量化→効果順に setDT 同期削減/小カーネル融合/(必要なら)CUDA Graph を A/B 実測で採否判断。過去「sync 撤廃が効かなかった」理由もデータで検証 |
| [architecture-residual-monitor-async.md](architecture-residual-monitor-async.md) | architecture | done | [`docs/architecture/`](../../docs/architecture/overview.md) | 定常 implicit が host 律速 (GPU 実働 ~50%) の主因=毎 step の残差 RMS (`thrust::transform_reduce` が変数ごとに host 返り=同期) と detectNaN (`thrust::any_of` 変数ごと同期)。**fused 1 カーネルで全保存量一括 sum-of-squares→device バッファ常駐**、`residualFlushInterval` step ごとに 1 回 D2H+CSV 書き出し (毎 step 粒度は不変)。detectNaN も fused device フラグ+`detectNaNInterval` 間引き。既定値は従来挙動。案3 (カーネル相乗り) は実測後に検討 |
| [discretization-median-dual.md](discretization-median-dual.md) | architecture | in_progress | [`docs/discretization/`](../../docs/discretization/) | cell-centered / node-centered (中点双対 median-dual) 両対応化。**核心**: GPU カーネル (対流/勾配/粘性/block-DPLUR) は「CV+面+接続」抽象だけで書かれ内部スキームは双対上で無変更で成立 → node 化は主にメッシュ前処理。`discretization: cell\|node` フラグで切替、`dualMeshBuilder` が双対量を HDF5 `/DUAL` に出力。書き換えは境界 (ゴースト→半割双対面 弱形式)・I/O・初期/プローブに局在。動機=tet 非構造の DOF/コスト削減 (≈1/5.5) と cell vs node 比較。M1 (Euler 1次) から段階実装 |
| [discretization-lsq-gradient.md](discretization-lsq-gradient.md) | gradient / architecture | in_progress | [`docs/discretization/`](../../docs/discretization/), [`docs/gradient/`](../../docs/gradient/) | node-centered (median-dual) 近壁で Green-Gauss 面勾配が **checkerboard** を持ち粘性 BL せん断を振動させるため、**重み付き最小二乗 (LSQ) 勾配**を node 限定 opt-in (`gradLSQ`, 既定 0) で導入。逆距離二乗重みの正規方程式 $M\mathbf g=\mathbf b$ を倍精度 3×3 (2D 自動 2×2) で解き、境界は bvar を半割面 LSQ 点として閉じる。cell / `gradLSQ=0` node はビット不変。**コード実装済・GPU 検証未** (中断セッションを正規化) |
| [discretization-node-boundary-ghostless.md](discretization-node-boundary-ghostless.md) | boundary / architecture | in_progress | [`docs/discretization/`](../../docs/discretization/) | node-centered 境界の**ゴースト全撤廃** (段階導入)。node では境界ノードが境界上に乗るためミラーゴーストが幾何不整合 → ① 壁ノード速度が厳密 0 にならない、② 壁∩出口コーナーで矛盾ゴースト 2 個 → exit-lip 発散 (case/29 viscous node)。**Phase 1**: 壁ゴースト撤廃＋壁 Dirichlet (u=0, 残差射影で陰解法整合)＋壁優先コーナー所有 (wall>inlet>outlet>slip)。壁せん断は内部双対面が担うのでゴースト不要。**Phase 2**: 残り (流入出/slip/axis) も半割面弱形式へ。親=discretization-median-dual |
| [architecture-node-centroid-value-position.md](architecture-node-centroid-value-position.md) | architecture | draft | [`docs/discretization/`](../../docs/discretization/), [`docs/diffusion/`](../../docs/diffusion/) | node-centered の `CELLS/centCoords` を「値の位置=ノード座標」に統一 (cell/node 同一処理, ncx 別配列を作らない)。双対 CV 重心は別量 (`r_eff=revolved_vol/A_planar`) に分離し軸対称 r 重み/source だけが参照、`axisCentroidShift` 撤去。**node モードの ghost を完全撤廃**し境界は内部隣接ノードの `∇φ·S` 弱形式 (= node-boundary-ghostless Phase 2)。真因: flux dcc が双対重心を使い近壁で見かけ非直交 `1/cosθ` max1.46e6→omega 拡散爆発 (node 座標で 1.00)。隣接ノード情報は保有確認済 (全壁ノード内部隣接2-3個)。Stage1 (壁スカラ ghostless) 実装済 |
| [diffusion-node-scalar-nonortho-limit.md](diffusion-node-scalar-nonortho-limit.md) | diffusion | **superseded** | [`docs/diffusion/`](../../docs/diffusion/) | SST omega 爆発の真因=flux dcc が双対重心で近壁見かけ非直交 (max1.46e6) と特定。当初の over-relaxed リミッタ案・ncx 別配列案は棄却し、**centCoords=node 統一** ([architecture-node-centroid-value-position.md](architecture-node-centroid-value-position.md)) に置換 |
| [discretization-node-wall-implicit-dirichlet.md](discretization-node-wall-implicit-dirichlet.md) | boundary / time_integration | done | [`docs/discretization/`](../../docs/discretization/) | node 壁 no-slip の **implicit Jacobian Dirichlet 化** (SU2 `DeleteValsRowi` 相当)。現状は残差射影のみで block-DPLUR の壁運動量が近傍と連成 → solve が `Δ(ρu)≠0` を返し壁速度が drift (case/36 で `|roUx|` mean 5.6→34、KE 除去が持続的エネルギーシンク化)。既存 `axis_flag` in-Jacobian decouple 機構 ([timeIntegration_d.cu:837](../../solver_density_cuda/cuda_forge/timeIntegration_d.cu#L837)) を**壁の運動量3行 (index1-3) へ拡張** + `wall_flag_d` を渡すだけ。連続/エネルギー行は保持。enforceWallNoSlip/zeroWallMomentumResidual は decouple 後に実質 no-op 化。親=node-boundary-ghostless / 先例=axisym-axis-singularity |
| [diffusion-viscous-shear-flux.md](diffusion-viscous-shear-flux.md) | diffusion | done | [`docs/diffusion/`](../../docs/diffusion/) | 粘性せん断フラックスの修正 (内部面の法線項 `delta_x→delta` + 転置項 + 壁面 no-slip 抗力、軸平行格子で横方向粘性が落ちる不具合、SU2 検証済) |
| [diffusion-node-wall-viscous-distance.md](diffusion-node-wall-viscous-distance.md) | diffusion | done | [`docs/diffusion/`](../../docs/diffusion/) | node 壁摩擦応力 twall を内部双対面 (壁ノード↔内部ノード) の粘性力集約で算出 (`viscousFlux_wall_node_d`, `nodeWallStressEdgeKernel`)。旧 node twall は wallTreatmentSST+壁ノード u=0 で恒等0だったのを物理値に修正・場は不変 (§11)。法線距離 dcc 退化 (§1-10) は別軸保留、試作 floor フラグは撤去 |
| [diffusion-node-boundary-real-distance.md](diffusion-node-boundary-real-distance.md) | diffusion | done | [`docs/diffusion/`](../../docs/diffusion/) | node 境界半割面の k/ω・化学種拡散を、退化する ghost mirror / 暫定 `∇φ·S` 弱形式から **skip (流束を加えない)** に統一。Dirichlet は内部双対面 W↔I が実距離で拡散を担い境界ノードはピンで上書き、Neumann は $\partial_n\phi=0$ で半割面 0 が正。平板 case/26 で **Cf/u_τ/δ99 が ∇·S 版と完全一致** (差 0.00%)、k は前縁スリップ域で skip がより正 (∇·S は Neumann 漏れで過剰生成)。cell は `isNode` ガードで atomicAdd 床内ビット不変。species は検証ケース未整備 (k/ω 準拠) |
| [diffusion-turbulent-thermal-conductivity.md](diffusion-turbulent-thermal-conductivity.md) | diffusion | done | [`docs/diffusion/`](../../docs/diffusion/) | RANS エネルギー方程式に乱流熱伝導 $\kappa_t=c_p\mu_t/Pr_t$ ($Pr_t$=0.9) を追加。応力は $\mu_{\rm lam}+\mu_{\rm turb}$ を使うのに熱伝導が層流のみ→乱流境界層で散逸熱が逃げ場を失い断熱壁静温が全温を超過 overshoot (case/16 SST で 449K→293K に修正) |
| [gpu-implicit-plan.md](gpu-implicit-plan.md) | time_integration | in_progress | [`docs/time_integration/`](../../docs/time_integration/) | GPU pseudo-time implicit (matrix-free + 近似 Jacobian + block DPLUR)。制御フロー整理・古典 DPLUR 化・dual-time 受け入れ構造（§0 改訂メモ）。dual-time 本体は後続 |
| [convection-slau2-lowmach.md](convection-slau2-lowmach.md) | convection | done | [`docs/convection/`](../../docs/convection/) | `solver: SLAU2` を追加 (圧力束第 3 項のみ差し替え、質量流束不変)。実装・検証完了。**検証所見**: 低マッハのエネルギー残差フロアは SLAU2 では解消せず (カップリングは共通の質量流束項。SLAU2 は衝撃波ロバスト性向け)。根治は前処理が必要 |
| [convection-pep-scheme-survey.md](convection-pep-scheme-survey.md) | convection | draft (**調査専用**) | [`docs/convection/`](../../docs/convection/), [`docs/thermophysics/`](../../docs/thermophysics/) | **PEP (Pressure-Equilibrium-Preserving) 系スキームの技術調査** (コード変更なし)。文献 (deep-research 97 agents/24 claims) + forge 現状を統合。**2 系統**: (A) 多成分・接触面 (double-flux: Abgrall/Karni, quasi-cons WENO: Johnsen-Colonius, DG-PEP: Ching) と (B) KEEP/split form (Kuya-Totani-Kawai, Shima-Kuya-Wada)。**2025-26 で融合** = 質量流束に $\alpha=(\partial\rho e/\partial\rho)_p$ で結ぶ **face 内部エネ流束補正**で**厳密保存のまま PEP** (Coppola 2026 / DeGrendele 2025, NASA 多項式 TP ネイティブ)。**最優先軸=非構造 FV**: ほぼ全研究が構造 FD/DG、例外は Kuya 2023 (非構造 KEEP-FV だが cell-vertex) と Badrkhani 2025 (OpenFOAM だが非保存)。**筆頭候補=完全保存 PEP の face $\rho e$ 補正を既存 `KEEP` convMethod に接ぎ木** (厳密保存→衝撃/Jacobian 両立, TP 整合)。**注意**: 非構造 cell-centered への再導出が要 (テンプレ Kuya 2023)、陰解法整合は文献皆無、Ranocha-Gassner: **PEP≠線形安定**。着手は contact-pressure plan の Gate 2 確定後 |
| [convection-multispecies-contact-pressure.md](convection-multispecies-contact-pressure.md) | convection | draft | [`docs/convection/`](../../docs/convection/) | 多成分 TP 接触面 (He/空気) の高 CFL 擬似時間 limit-cycle。現状 SLAU は **mixed-order face-state** (ρ,p,u は 2 次・組成 Y はセル 1 次)。**核心**: ① 1D 材料接触面ベンチで原因を切り分け (Baseline A vs 全量 1 次 Case D)、② face composition を flow と整合 (2 次再構成・thermo/flow flux/species flux で同一)。**助言からの差分**: 重い species 2 次実装の前に **1D ゲートを先行**。既存診断 (`FORGE_CONTACT_1ST/BLEND`) で **flow を contact で 1 次化すると振動増・relax で低減** → Gate 2(保存形 PEP 不足)/Gate 4(defect-correction)を示唆。double-flux/完全 PEP は本計画外 (Gate 2 確定時に次 issue) |
| [time_integration-lowmach-preconditioning.md](time_integration-lowmach-preconditioning.md) | time_integration | done | [`docs/convection/`](../../docs/convection/), [`docs/time_integration/`](../../docs/time_integration/) | Weiss–Smith 低マッハ前処理 (`lowMachPrecond`, 既定 0)。**フラックス散逸前処理 `c_hat→c'` のみ採用** — `case/23.axi_nozzle` 3D ノズルの低マッハ自励振動 (limit cycle) を 20k step 収束ベースで ε=0.15 −32% 減衰 (超音速域不変)。**LHS 固有値前処理・setDT 前処理は block DPLUR で対角優位性を崩し有害→棄却**。設計分析 (§9 `2026-06-08`) で「振動=RHS 駆動」と確定し **Phase 3 (b) Thornber 再構成補正 (`lowMachThornber`) を実装・20k 検証 → 負の結果** (速度ジャンプを減らす=散逸を減らす逆符号の補正で、under-damping のこの症状には無効〜僅かに悪化。機能は opt-in 残置・根治不採用)。根治レバーは Phase1 の圧力散逸 `c'` 側に残り、ε 安定限界超えは完全 (a) に帰着。**Phase 4 (a) 完全 $\Gamma^{-1}A$ 前処理 (`lowMachPrecond=2`) を実装・検証 → 低マッハ自励振動を根治 (採用)**: $\Gamma_c$ ランク1閉形の時間項+物理フラックス FVS+前処理 $\Delta\tau'$、対角 5×5 を倍精度組み立て・解。Phase1 では発散した ε=0.05 を前処理 LHS が安定化し、chamber 振幅 0.882%→**0.087%・定常収束**(超音速域不変)。収束加速は副次でブレークイーブン。plan **done** |
| [architecture-axisym-axis-singularity.md](architecture-axisym-axis-singularity.md) | architecture | done | [`docs/time_integration/`](../../docs/time_integration/) | 軸対称近軸固着の**真因を更新 (§0, 2026-06-14)**: 精度ではなく **block-DPLUR 粘性対角の幾何不整合** (`face_area·(2ν/delta)` が ≈2ν に潰れ近軸で r 重み喪失・ゼロ面積面にスプリアス) が真因。residual 整合形 `2ν·|S|²/(Δcc·S)` への**幾何是正で float のまま固着解消** (第一セル u_r +1.4→+17.9, double solve と一致)。フープ/KL/dilatation は無関係 (マスク)。double solve (`implicitSolvePrecision=1`) は対症療法→保険手段に降格 |
| [precision-mixed-axisym.md](precision-mixed-axisym.md) | precision | **superseded** | [`docs/time_integration/`](../../docs/time_integration/) | double solve (`implicitSolvePrecision=1`) を root-fix としたが**根治ではなかった** (§0)。真因は粘性対角の幾何不整合 (→architecture-axisym-axis-singularity §0) で、幾何是正 (float) が根治。本フラグ (0=float / 1=solve のみ double) は**検証/保険**として残す。閉形式 FVS (~10%高速・軸対称 legacy 等価) の既定化は有効 |
| [turbulence-kato-launder.md](turbulence-kato-launder.md) | turbulence | done | [`docs/turbulence/`](../../docs/turbulence/) | SST 生産項の stagnation/加速アノマリー対策。strain-based `Pk=μt S²` は非回転の強加速 (ノズル喉核) で偽生産。`dilatationCorrection` (等方分除去) では消えない別系統の欠陥。Kato–Launder `Pk=μt S Ω` を opt-in (`katoLaunder`, 既定 0) で実装・検証。**3D 90°セクタ検証**: 2D の軸中心**鋭いスパイク**は 3D で再現せず=軸対称の軸面積→0 封じ込め由来の増幅。ただし**核全体の strain-dominated な μt 過大 (本体) は 2D/3D 共通=幾何非依存**。検証 (`run_0017` off vs `run_0018` on): 軸 k 17→1.9・核 k 5.0→0.47 を除去、**壁 BL μt・推力 (mdot/λ) 不変**。`katoLaunder:0` 既存とビット一致 |
| [architecture-axisymmetric.md](architecture-axisymmetric.md) | architecture | done | [`docs/axisymmetric/`](../../docs/axisymmetric/) | 軸対称モード Phase 1 (B 流儀 r 重み付け + 圧力ソース + 軸 BC、非粘性) |
| [architecture-axisym-nozzle-geometry.md](architecture-axisym-nozzle-geometry.md) | architecture | done | [`docs/axisymmetric/`](../../docs/axisymmetric/) | 軸対称検証ノズルの喉部直後を 5 次多項式で滑らかに接続する geometry 改善 |
| [architecture-rans-sst.md](architecture-rans-sst.md) | architecture | done | [`docs/turbulence/`](../../docs/turbulence/) | Menter SST (k-ω) を explicit 軸対称ノズル (run_0087〜0090) で 4 段階検証完了。advection・diffusion・source (F1/F2 ブレンド)・渦粘性すべて実装済。軸対称 geometric source は子 plan へ |
| [turbulence-enhanced-wall-treatment.md](turbulence-enhanced-wall-treatment.md) | turbulence | done | [`docs/turbulence/`](../../docs/turbulence/) | SST automatic (enhanced / y⁺ 非依存) wall treatment を opt-in (`wallTreatmentSST`, 既定 0) で導入。現行は low-Re 壁解像型のみ (`60ν/β₁y²`) で第一セルがバッファ/対数層に落ちると ω・Cf を誤る。**3 本柱**: ① 摩擦速度 u_τ を **Reichardt 普遍速度則の逆解き** (Newton 数回, 粘性低層・バッファ・対数を 1 式で接続) ② ω 壁 BC を **Menter automatic ブレンド** `√(ω_vis²+ω_log²)` (ω_vis=6ν/β₁y², ω_log=u_τ/√β\*κy) ③ 運動量壁せん断を modeled `τ_w=ρu_τ²` (有効壁粘性 μ_eff=ρu_τ²y/U_t, y⁺→0 で μ_eff→μ 壁解像に縮退・対数層で壁関数化)。新カーネル `ransWallFunction_d.*` が u_τ を解き bvar 共有。検証=`case/26.flat_plate_sst` を 3 帯 (y⁺≈0.35/10/30) で y⁺ 非依存性実証。既定 0 はビット不変 |
| [architecture-axisym-sst.md](architecture-axisym-sst.md) | architecture | done | [`docs/turbulence/`](../../docs/turbulence/) | 親 SST の子 plan。フープひずみ $2(u_r/r)^2$ (run_0091) + 生産項の圧縮性補正 `dilatationCorrection` (0:off/1:deviatoric/2:+等方項) を実装・段階検証。run_0093(A+B) で k −12%・vis_turb −14% の膨張減衰を確認。既定値 2 (全 SST ケースに適用) |
| [time_integration-explicit-pointimplicit-sst.md](time_integration-explicit-pointimplicit-sst.md) | time_integration | done | [`docs/time_integration/`](../../docs/time_integration/) | 陽解法 RK (`timeIntegration==1/3`) のスカラー (k/ω) 源項を消散ヤコビアン (`src_jac_k`/`src_jac_omega`) で point-implicit 減衰。block 陰解法と同じ対角を残差増分に適用し、純陽的だと壁近傍 ω で発散する RANS を陽解法 RK で安定化。LES (src_jac=0) は無影響。`case/18.backstep` 2D で検証 |
| [time_integration-scalar-dplur-axisym-source.md](time_integration-scalar-dplur-axisym-source.md) | time_integration | done | [`docs/time_integration/`](../../docs/time_integration/) | scalar DPLUR (`blockDPLUR==0`) の LHS に軸対称フープ源 `res_roUy += (P−τθθ)·A_planar` の Jacobian 対角 `A_pl·((γ−1)u_y + 2μ/(ρ r_eff))` を陰化し block と整合 (per-cell γ で **TP 対応**, 非負クランプ・定常不動点不変)。**検証で確定: これは整合改善であって case/29 の scalar 発散の主因ではない** — 真因は出口コーナーの 2 次 MUSCL オーバーシュートを scalar の式分離・1 次凍結 LHS が減衰できないこと (1 次 scalar は cfl2 でも収束, block は 2 次で安定)。実用指針: 2 次の陰解法は block、scalar は 1 次限定 |
| [time_integration-implicit-stable-cfl.md](time_integration-implicit-stable-cfl.md) | time_integration | done | [`docs/time_integration/`](../../docs/time_integration/) | 陰解法 (block DPLUR) の安定 `cfl_pseudo` 引き上げ。診断で律速は **k/ω 輸送項の陽的扱い**と確定 (平均流は静止し k/ω のみ発散・`scalarDiffusion=0` で消失)。k/ω 移流+拡散のスペクトル半径を point-implicit 対角 `transport_diag` に陰化 (defect-correction で定常解不変)。`case/26.flat_plate_sst` で安定 `cfl_pseudo` 5–6→**120** (約20倍)、壁法則・Cf は <0.1% 不変。生産項陰化 (#1) は ceiling 不変で棄却、平均流粘性 (#2) は律速でなく無効 |
| [architecture-detect-nan.md](architecture-detect-nan.md) | architecture | done | [`.github/forge-solver-settings.md`](../forge-solver-settings.md) | NaN/Inf 検知診断オプション `detectNaN` (既定 0)。`1` で毎ステップ終端に保存量+P を `thrust::any_of` 検査し、非有限値があれば `res_nan_<step>.h5` を強制ダンプして `exit(1)`。`05.sod_shock_tube` の発散/安定 2 ケースで検証済 |
| [thermophysics-multicomponent-tpgas.md](thermophysics-multicomponent-tpgas.md) | thermophysics | done | [`docs/thermophysics/`](../../docs/thermophysics/) | 多成分 thermally-perfect gas (非反応)。**M1 NASA-9熱力学+SLAU/ROE TP整合 / M2 多成分質量分率輸送 / M3 kinetic theory輸送係数 / M4 化学種拡散+エネルギー結合 を実装・検証完了** (衝撃管CEA照合・拡散係数0.987一致・27.axi_nozzle_plumeでTP-N2がNASA等エントロピーに0.04%一致)。TP境界条件整合化・陰解法Jacobianのγ[ic]化も完了。後続: 組成依存BC・RANS+TP検証 (plan §10) |
| [time_integration-general-eos-jacobian.md](time_integration-general-eos-jacobian.md) | time_integration / thermophysics | done | [`docs/time_integration/`](../../docs/time_integration/), [`docs/thermophysics/`](../../docs/thermophysics/) | **TP 陰解法律速の根治**。block-DPLUR の 5×5 flux Jacobian 閉形式 FVS が CPG 専用の EOS 関係 (`h=c²/(γ−1)`, `∂P/∂ρ\|_e=0`) を前提とし、TP では別方程式を線形化 (FD 検証で TP 誤差 469%)。一般EOS固有系 (音響 `H_t±cU_n`・接触 `H_t−c²/κ`・せん断) を実 `H_t`・`κ=γ−1`・`χ=c²−κh` で構築し真の ∂F/∂Q に整合化。Method A=数値 `L=R⁻¹` (部分ピボット double 5×5 LU) 先行、CPG 経路は閉形式のまま分岐保持 (回帰基準)。同一セル・同一 thermo で `H_t/c²/κ/χ` 評価 (セル/面混在禁止)。検証 Level1〜4 + 残差床は別トラック。CPG/TP 対照 (case/16 run_0121〜0155) で真因確定済 |
| [thermophysics-species-implicit-coupling.md](thermophysics-species-implicit-coupling.md) | time_integration / thermophysics | done | [`docs/thermophysics/`](../../docs/thermophysics/), [`docs/time_integration/`](../../docs/time_integration/) | 多成分 TP 陰解法 (block-DPLUR) の要因2 打ち手「案B (緩和整合)」を実装・検証。化学種 `ρY_s` を流れ block と**同一緩和** (同一凍結残差・`dt_local`・`implicitRelax`・`nStepInner` sweep) のスカラ DPLUR で前進 (config `speciesImplicitCoupling`, 既定 0=従来・ビット不変)。**結論=案B は無効**: matched≡off で cfl 上限が一切動かず、単成分 N2 も同 cfl4 で発散=ボトルネックは流れ/SST 律速・組成は完全一様で `res_roY≈0`。要因2 残ペナルティは EOS cross-Jacobian (案A) 領域だが流れ/SST 上限が先に律速。flag は温存 (組成勾配の大きいケース向け)。検証 `case/16.nozzle_wys` run_0110〜0120 |
| [su2-nemo-contact-thermo-investigation.md](su2-nemo-contact-thermo-investigation.md) | convection / thermophysics | draft | [`docs/thermophysics/`](../../docs/thermophysics/), [`docs/convection/`](../../docs/convection/) | **調査専用 (コード変更なし)**。SU2-NEMO v8.5.0 (`12eb826`, `.external/su2-src/` に取得) が多成分 contact で組成・温度・流束・陰的 Jacobian をどう扱うかを実コード根拠で棚卸し、forge の mixed-order face-state (`T_f=P_f^{(2)}/(ρ_f^{(2)}R(Y_cell^{(1)}))`) と比較。**核心**: SU2 は species を flow と**同次 (2次・単一 limiter) で co-reconstruction** し forge の次数混在を持たない (`CNEMOEulerSolver.cpp:551-576`)。ただし face `T,P,h` を EOS 再回復せず別種非整合は残り、**double-flux / pressure-equilibrium 補正は皆無** (全 grep ヒットなし) → 偽圧振動に晒される。Jacobian は full `nVar×nVar` で species⇔flow を `dPdU` 連成 (`CNEMONumerics.cpp:153-216`)、近軸 extra spectral radius は無し。forge への取り込み優先度=①species 同次再構成 ②非物理 face 1次 revert ③`∂P/∂ρ_s` 連成。double-flux は SU2 にも無く forge 独自検討が必要 |
| [condensation-nonequilibrium.md](condensation-nonequilibrium.md) | condensation | in_progress | [`docs/condensation/`](../../docs/condensation/) | 非平衡凝縮 (4 モーメント方程式 $\rho g,\rho Q_2,\rho Q_1,\rho Q_0$)。Lin 2014 の CNT+Iland 核生成 / 修正 Gyarmathy(Goodheart) 成長 / Hill $T_d$ / method of moments を、RANS roK/roOmega の追加保存スカラー骨格を流用して実装。種ごとに核生成/成長/物性を config 切替 (N2=CNT_Iland+Goodheart / H2O=CNT_Kantrowitz+Hertz–Knudsen)。**Phase 1**=4 モーメントを受動スカラー (ソース=0) として輸送し case/34 dry 回帰一致 (本セッション)。**Phase 2**=N2 凝縮物理+二相 EOS $p=(\rho-\rho g)RT$+point-implicit ソース+$\mu_n$ 無次元化 (float)→case/34 静圧上振れ。**Phase 3**=H2O+case/16 Wyslouzil。設計: 受動スカラー SLAU 優先 / NS と分離 segregated / 圧力新項 $\xi_g=-RT+\kappa(L-RT)$ |
| [verification-passive-pseudoshock-control.md](verification-passive-pseudoshock-control.md) | verification | in_progress | [`docs/convection/`](../../docs/convection/), [`docs/boundary/`](../../docs/boundary/) | Matsuo et al. 1988 の多孔壁+キャビティ境界層パッシブコントロールを 2D 逆解析 (`case/36`)。擬似衝撃波が弱化しショックレスに近づくか再現確認。形状確定 (Fig.2/3)・入口 M=1.689 算出・2D メッシュ生成器 (`make_mesh.py`, porous 63.6k / solid 38.3k quad) 完了。次: 層流で超音速場確立→背圧掃引。10°ディフューザ+チャンバ・SST は後段 |

## 状態の意味

- `draft` — 設計検討中、実装未着手。
- `in_progress` — 実装または検証進行中。
- `done` — 関連 docs 更新・実装・検証がすべて完了。
- `superseded` — 別 plan に置き換えられた。後継 plan を `related_plans` に明記すること。
